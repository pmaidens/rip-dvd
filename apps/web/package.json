{
  "name": "@rip-dvd/web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "next build",
    "dev": "next dev",
    "pretest": "node ../encode-worker/scripts/build-atomic-exchange.mjs",
    "start": "next start",
    "test": "tsc -p ../../packages/config/tsconfig.build.json && tsc -p ../../packages/data-access/tsconfig.build.json && vitest run",
    "test:browser": "playwright test",
    "test:browser:serve": "next build && node test/browser/seed-catalog-review.mjs && next start --hostname 127.0.0.1 --port 3100",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@rip-dvd/config": "workspace:*",
    "@rip-dvd/data-access": "workspace:*",
    "next": "16.2.11",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@types/react": "19.2.17",
    "@types/react-dom": "19.2.3",
    "happy-dom": "20.11.2"
  }
}
